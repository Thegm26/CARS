{
  "task_id": "returns-bulk-approve",
  "touched_files": [
    "src/cars_returns/app.py",
    "src/cars_returns/service.py",
    "tests/regression/test_returns_http.py",
    "tasks/returns-bulk-approve/task.json"
  ],
  "acceptance_mapping": {
    "AC1": "The new bulk approval route approves multiple pending return requests for managers in a single call.",
    "AC2": "The bulk approval route requires the support_manager role and rejects unauthorized access.",
    "AC3": "The bulk approval response reports invalid request ids and only approves valid pending requests."
  }
}
